# Generated by scripts/configure.py
name: build
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
jobs:
  build:
    uses: robertvazan/rvscaffold/.github/workflows/net-build.yml@master
    with:
      dotnet-version: 6.0

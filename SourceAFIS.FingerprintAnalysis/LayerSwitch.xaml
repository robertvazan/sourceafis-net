<UserControl x:Class="SourceAFIS.FingerprintAnalysis.LayerSwitch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SourceAFIS.FingerprintAnalysis" 
             xmlns:v="clr-namespace:SourceAFIS.Visualization;assembly=SourceAFIS.Visualization"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Name="LayerSwitchControl">
    <UserControl.Resources>
        <v:EqualsToVisibilityConverter x:Key="VisibleIfEquals" />
    </UserControl.Resources>
    <Canvas DataContext="{Binding ElementName=LayerSwitchControl}" Width="{Binding ExtractionData.Width}"
            Height="{Binding ExtractionData.Height}" ClipToBounds="True">
        <v:FloatArrayImage InputArray="{Binding ExtractionData.Equalized}"
                           Visibility="{Binding Layer, Converter={StaticResource VisibleIfEquals}, ConverterParameter={x:Static local:Layer.Equalized}}"/>
    </Canvas>
</UserControl>
